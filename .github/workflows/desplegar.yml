name: Desplegar a AWS y K8s

on:
  workflow_run:
    workflows: ["Pipeline CI"]
    types: [completed]
    branches: [master]

jobs:
  despliegue:
    # Solo se ejecuta si el Pipeline CI terminó con éxito
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
    - name: Descargar código
      uses: actions/checkout@v3

    - name: Configurar credenciales de AWS
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Actualizar Kubeconfig
      # Conexión al clúster 'microservicios-cluster' en AWS
      run: aws eks update-kubeconfig --name microservicios-cluster --region us-east-1

    - name: Desplegar en Kubernetes
      run: |
        # CORRECCIÓN: Se cambió .yaml por .yml para que coincida con tu archivo real
        kubectl apply -f ./deployment.yml
        # Reinicia el despliegue para asegurar que tome la última imagen de Docker Hub
        kubectl rollout restart deployment productos-api-deployment